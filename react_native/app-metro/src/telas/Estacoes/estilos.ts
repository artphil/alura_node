import { StyleSheet } from 'react-native';
import { cores } from '../../estilos';

export default StyleSheet.create({
  titulo: {
    color: cores.claro,
  },
  container: {
    alignItems: 'center',
  },
});
